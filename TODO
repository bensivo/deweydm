Views:
- Add 'order by' to views, so you can save sorting by a specific column
- Add 'column' to views, so you can only show specific columns
- In filters, add option 'is one of' or 'is not one of' for select options.


- Bundling into an electron app with a sqlite database
- Filters on the all-entities view
- Sort by on the all-entities view
- Views, which are just pre-created sets of filters and sorting (may replace lists)
- Make the searchbar work
- MD field type
- Attachment field type
- Bulk edit mode in all-entities view

- Lists:
  - Make the lists page look like a table, with a header and columns that line up
  - Move items between lists
  - Remove items from a list
  - A 'kanban' view, where each list is a column and you can drag between them.
    - could just be a list with 'sections', and you can drag items between sections.
    - could be a list that has 'sections' mapped to a specific select field.
