Features in order:
- feature: 'Workspaces' to have entirely differnet sets of data hierarchies. 
- feature: a "Comments" or "Notes" field (or just feature not related to fields).
- feature: add 'order by' to entity-list page, esp. multiple order-bys stacked. 
- feature: save column visibility settingsm, esp in views
- feature: in 'backlink' view, select some columns to show (e.g. in "story" show task name and status)
- tweak: reference uses plural name of entity, not singular
- bug: column selection doesn't persist
- feature: Add helpertext/descriptions to fields, for inputs
- feature: Split panel view, instead of just a table
- tweak: Make table columns resizable
- feature: a separate 'backlog' list, which allows users to maintain a custom ordering of items
- feature: in the entity-list page, if showign a backlink type, let the user expand the list somehow to see all of them?
- feature: better UI to bulk create a bunch of sub-items from a parent
- feature: update status fields in less clicks
- tweak: open entity page in edit mode by default